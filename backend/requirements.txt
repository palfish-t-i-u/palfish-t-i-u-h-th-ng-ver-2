# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Supabase / DB
# Pin chính xác (M2-T0, plan pr-list-server-pagination) — tránh Render pull bản mới
# có breaking change giữa các lần deploy. 2.30.0 = bản đang cài + test pass thật trên
# máy dev (901/907 backend test xanh) tại thời điểm pin — không phải đoán theo version
# cũ trong plan gốc (2.15.2, chưa verify chạy được).
supabase==2.30.0

# HTTP client
httpx>=0.27.0

# Env
python-dotenv>=1.0.0
tzdata>=2024.1

# Excel generation (Module 4 — xuất hóa đơn thuế)
openpyxl>=3.1.0

# Data processing (Module 5 — xuất master data CRM)
pandas>=2.2.0
xlrd>=2.0.1

# Google Sheets — import All File Thu Hiền → Sổ doanh thu (docs/M5_GSHEET_IMPORT.md)
google-auth>=2.29.0
google-api-python-client>=2.127.0
cryptography>=42.0.0

# Image thumbnails cho tin DingTalk báo đơn (bill-thumbs bucket, 18/7)
Pillow>=10.0.0

