[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=line
markers =
    auth: Auth/permission tests (Đạt)
    db: Database/race-condition tests (Đức)
    other: Webhook/encrypt/config tests (Giang)
