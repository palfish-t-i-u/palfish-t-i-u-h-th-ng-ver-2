# Bỏ qua thư mục chứa file mẫu / prototype
references/
_prototype_ref/
CRM Palfish*/

# Brief nội bộ (PDF + script tạo PDF — không commit)
docs/Brief_Module3_Module4_xuat_hoa_don.pdf
docs/feedback_9_6_2026.pdf
scripts/gen_module34_brief_pdf.py
scripts/deploy-hooks.local

# Backup/artifact từ cleanup scripts
docs/artifacts/

# Deploy hook secrets (Render) — local only, KHÔNG commit
scripts/deploy-hooks.local

# Node modules (root level)
node_modules/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local
frontend/.env.sandbox
frontend/.env.development.local
frontend/coverage/
frontend/e2e-results/
frontend/e2e-report/
frontend/e2e/.auth/
frontend/.env.e2e
.vite/

# Editor / máy cá nhân
.env
.vscode/settings.json

# Backend
backend/__pycache__/
backend/**/__pycache__/
backend/*.py[cod]
**/*.pyc
backend/.venv/
backend/venv/
backend/.env
backend/.env.sandbox
api_pipe/.env
**/secrets/
*gsheet*.json
*service-account*.json
# Backup/report dọn trùng Sổ doanh thu (chứa dữ liệu khách hàng)
backend/backups/
api_pipe/__pycache__/
api_pipe/*.py[cod]
api_pipe/ngrok*

# Backup/báo cáo tự sinh từ dedup Sổ doanh thu — giữ local, không commit
backend/backups/

# OS
.DS_Store
Thumbs.db

# Local AI skills — chỉ chạy trên máy cá nhân (RTK, caveman, cavemem)
.agents/
.claude/*
# Exception: project-specific agent definitions và skills (shared across team)
# Dùng .claude/* (không phải .claude/) để git cho phép descend vào subdir được un-ignore
!.claude/agents/
!.claude/agents/*.md
!.claude/skills/
!.claude/skills/**
skills-lock.json
.cursor/rules/caveman.mdc
.clinerules/
.windsurf/
.opencode/
.github/copilot-instructions.md
AGENTS.md
RTK.md
.cavemem/
docs/feedback_9_6_2026.pdf

# MCP server config — chứa app_secret (Lark, Supabase, etc), giữ local
.mcp.json
