# KẾ HOẠCH CÔNG VIỆC — 30/05/2026

> **Repo:** `palfish-t-i-u/palfish-t-i-u-h-th-ng-ver-2` · **Branch:** `main` @ `ca83253`
> **Codebase:** `E:\PalFish\DA\pf-gmv-reconciliation\palfish-t-i-u-h-th-ng-ver-2`

---

## TRẠNG THÁI 9 TASK HÔM QUA (29/05) — CẬP NHẬT 30/05 10:00

| # | Task | Tiến độ | Ghi chú |
|---|---|---|---|
| 1 | Phân quyền 3 cấp | ✅ **DONE** | BE `e8bc2bf` (Đức). FE `6d123b9` (Minh). Merged main. |
| 2 | PayOS HCM | ⏸️ **HOLD** | PayOS không hỗ trợ VCB. Chờ Casso (Task 8). |
| 3 | Xoá test + import data | ✅ **DONE** | Cleanup SQL on main. Import fix `73f1950` (Minh). **Data chưa nạp** — cần bấm Sync Data sau deploy hoặc chạy sync-gsheet. Dry-run OK: 96 delta rows. |
| 4 | Sandbox | 🔧 **~70%** | Code done (Giang `01c9dc4` + Đức `be2fabc`/`3bbf390` + Minh `a581d9a`). **Chờ Đức provisioning** infra. |
| 5 | Redesign Kích hoạt (Pulse) | ✅ **DONE** | `20a5884` Pulse UI: avatar, toggle, count progress, tổng tiền cam, viền accent, monospace course code. Codex + Claude. |
| 6 | Module thống kê | ⬜ → **Chuyển thành Task 1 hôm nay** (Bảng thông tin) | Anh Hiếu cung cấp mẫu HTML. |
| 7 | Tốc độ load | ✅ **DONE** | `ee8011f` (Giang). ~4s. |
| 8 | Casso Flow | 🔧 **~25%** | MB connected. Chưa code. ⚠️ Deadline ký HĐ 3 bên ~05/06. |
| 9 | Fix 2 lỗi FE | ✅ **DONE** | Lỗi A: allocation guard `20a5884`+`98454cc`+`e5fc651`+`6d16250`. Lỗi B: disabled orderId removed `2617bf1`. Scroll fix `ca83253`. Remove UID `6d16250`. Edit hint `e59e0db`. |

### Commits hôm nay trên main (30/05, đã push):
```
ca83253  fix(ux): remove ar-uid-list scroll trap — drawer body scroll naturally
f438116  fix(ux): expand ar-uid-list height
6d16250  fix(task9): add remove-UID button + scrollable mini card body
dbe8e64  fix(m5): sync Sổ doanh thu when B3 course gets CRM order_id (Đức)
e59e0db  fix(ux): pencil icon in edit hint
e5fc651  fix(task9): allow edit when locked but remaining > 0
98454cc  fix(task9): close allocation loopholes in ActiveRequestMiniCardV2
20a5884  feat(task5+9): Pulse redesign + allocation guard
```

---

## 3 TASK HÔM NAY (30/05)

### Task 1 — Module "Bảng thông tin" (TOP 2 · Giang/Đức/Đạt BE · Minh FE)

**Mục đích:** Tạo lý do để sales vào app thường xuyên — dashboard cá nhân với vinh danh, xếp hạng, nhiệm vụ.

**Mẫu thiết kế:** `C:\Users\silly\Downloads\Bảng thông tin (standalone).html` (file HTML từ Claude Design của anh Hiếu)

**4 vùng trong Bảng thông tin:**

| # | Vùng | Nguồn dữ liệu | Ghi chú |
|---|---|---|---|
| 1 | **Tính hoa hồng** | Chưa có — để trắng | Hiện text "Đang phát triển". Anh Hiếu cung cấp công thức sau. |
| 2 | **Vinh danh hôm nay + Bảng xếp hạng tháng** | Bảng `so_doanh_thu` | Query tổng GMV/VND theo sale hôm nay → top 1 vinh danh. Tổng tháng → ranking. |
| 3 | **Bảng nhiệm vụ & thưởng tuần** | Static content | Copy y nguyên nội dung từ mẫu HTML. |
| 4 | **Bảng sự kiện nội bộ** | Static content | Copy y nguyên nội dung từ mẫu HTML. |

**FE:** Tạo `frontend/src/components/DashboardTab.tsx` (hoặc `BangThongTinTab.tsx`). Thêm vào `MainPage.tsx` — sidebar item "Bảng thông tin" đặt lên ĐẦU TIÊN trong nav (trước "Quản lý thanh toán"). Mọi role đều thấy.

**BE:** Endpoint `GET /dashboard/bang-thong-tin` hoặc tái dùng `GET /revenue/pivot/sales-performance` + filter.

### Task 2 — Điều tra sync Sổ doanh thu khi kích hoạt khóa học (TOP 1 · Đức)

**Vấn đề:** Kích hoạt khoá học xong (PATCH order_id) mà không có doanh thu mới trong Sổ doanh thu.

**Đức đã commit `dbe8e64`:** Thêm `sync_ledger_from_ar_course` trong `revenue_routes.py`, gọi sau PATCH course order_id trong `activation_routes.py`. **Cần verify** commit này hoạt động đúng — test kích hoạt → check Sổ doanh thu có dòng mới không.

**Files:**
- `backend/activation_routes.py:1185` — gọi `sync_ledger_from_ar_course` khi `order_id` set
- `backend/revenue_routes.py` — hàm `sync_ledger_from_ar_course` mới (223 dòng thêm)
- `scripts/seed_so_doanh_thu.py` — seed data update

### Task 3 — Đổi icon app + favicon + tên miền (TOP 3 · Minh)

**Icon/Favicon:**
- **In-app icon** (sidebar logo): `C:\Users\silly\Downloads\Avatar 5.png`
- **Favicon** (browser tab): `C:\Users\silly\Downloads\Avatar 3.png`
- Hiện tại: `frontend/public/favicon.svg` (SVG) + `frontend/index.html:5` link icon
- Cần: convert PNG → SVG hoặc dùng PNG trực tiếp, thay thế favicon.svg

**Tên miền:**
- Hiện tại: `https://palfish-gmv-manager.vercel.app/`
- Mong muốn: domain doanh nghiệp (vd: `palfish.gmv.manager.com` hoặc tương tự)
- Anh Hiếu có tài khoản iNET Portal (quản lý domain `palfish.vn`): `https://portal.inet.vn/list-service`
- Domain `palfish.vn` đang active (hết hạn 10/06/2027)

**Hướng đi tên miền:**
- **Option A (miễn phí, khuyến nghị):** Dùng subdomain của `palfish.vn` → `gmv.palfish.vn`
  - Vào iNET DNS → thêm CNAME record `gmv` → `cname.vercel-dns.com`
  - Vercel Dashboard → Settings → Domains → Add `gmv.palfish.vn`
  - SSL tự động qua Vercel
- **Option B (trả phí):** Mua domain mới (vd: `palfish-gmv.com`) → ~$10-15/năm
- **Option C:** Dùng subdomain Vercel khác (miễn phí nhưng vẫn `.vercel.app`)

---

## RÀNG BUỘC

- ⚠️ Import Sổ doanh thu: fix đã deploy trên main. Có thể bấm "Sync Data" trên UI production an toàn (dedup `import:%` đã hoạt động).
- ⚠️ Casso Task 8: chưa code, deadline ký HĐ 3 bên ~05/06.
- ⚠️ `main` local = `origin/main` = `ca83253` — fully synced, worktree clean.
